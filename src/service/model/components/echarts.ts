import * as echarts from 'echarts';

export declare type RendererType = 'canvas' | 'svg';
export declare type EChartsOption = echarts.EChartsOption;
export declare type ECharts = echarts.ECharts;

export interface Size {
  with: number,
  height: number
}
